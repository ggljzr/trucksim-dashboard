import { Container } from "react-bootstrap";

const Footer = () => {
    return (
        <div>
            <footer>
                <Container className="Footer">
                </Container>
            </footer>
        </div>
    );
}

export default Footer
